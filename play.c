#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int n;
	char y;

	printf("please enter your age");
	scanf("%s", &n);
	   
	if
	{ 
		if(n==y)
		{	
			{printf("please insert a number");		
		}

	}
		else if(n >= 100)
	{
		printf("You are to old for this guy");
	}
	else{
		printf("You have spent%s",+ (365 * n) +"days on earth");
	}
	return(0);
}
